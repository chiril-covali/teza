{
  "slug": "parcurgere_adancime",
  "name": "Parcurgere în adâncime (DFS)",
  "category": "grafuri",
  "difficulty": "mediu",
  "summary": "Explorează graful mergând cât mai adânc posibil pe fiecare ramură înainte de backtracking.",
  "timeComplexity": {
    "best": "O(V+E)",
    "average": "O(V+E)",
    "worst": "O(V+E)"
  },
  "spaceComplexity": "O(V)",
  "visualization": {
    "kind": "graph"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "nodes": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "edges": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "from": {"type": "string"},
            "to": {"type": "string"}
          },
          "required": ["from", "to"]
        }
      },
      "start": {
        "type": "string"
      }
    },
    "required": ["nodes", "edges", "start"]
  },
  "defaultInput": {
    "nodes": ["A", "B", "C", "D", "E", "F"],
    "edges": [
      {"from": "A", "to": "B"},
      {"from": "A", "to": "C"},
      {"from": "B", "to": "D"},
      {"from": "B", "to": "E"},
      {"from": "C", "to": "F"}
    ],
    "start": "A"
  }
}
